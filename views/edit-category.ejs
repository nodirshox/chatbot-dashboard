<%- include('includes/header') %>
<div class="banner"><h2><a href="/">Bosh sahifa</a><i class="fa fa-angle-right"></i><span>Kategoriyani o'zgartirish</span></h2></div>

<div class="blank">
    <div class="blank-page">        

    <a href="/category" class="btn-default btn">Barcha kategoriyalarni ko'rish</a><br><br>
    <form action="/edit-category/<%= cat._id %>" method="POST">
        <div class="form-group">
            <label>Kategoryiya nomi</label>
            <input type="text" name="name" class="form-control" value="<%= cat.name %>" autocomplete="off" required>
        </div>
        <div class="form-group">
            <label>Izoh</label>
            <input type="text" name="description" class="form-control" value="<%= cat.description %>" autocomplete="off" required>
        </div>
        <button type="submit" class="btn btn-success">SAQLASH</button>
    </form>
</div>
</div>

<%- include('includes/footer') %>